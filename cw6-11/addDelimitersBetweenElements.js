// Input: Array of elements

// ["h","o","l","a"]

// Output: String with comma delimited elements of the array in the same order.

// "h,o,l,a"

const printArray = array => array.join()

let test = [2,4,5,2];

console.log(printArray(test));

// Understood the assignment. Pretty sure this is just the simplest method of completing the given task